using UnityEngine;
using System.Collections.Generic;

/*
 * This script handles the score for multiplayer games.
 * It contains a list of all the connected players
 * and this information is shown at the end of the round.
 */
public class ScoreScript : MonoBehaviour {

    //Dictionary<int, Player> players = new Dictionary<int, Player>();
    // Use this for initialization
    void Start () {
        // add all connected players to players list.
    }

    void IncreaseScore(int id)
    {

    }
    

}
